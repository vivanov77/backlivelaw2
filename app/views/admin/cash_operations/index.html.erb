<h1>Операции с наличными</h1>

<div style="color:red">ПРИМЕЧАНИЕ: Уже созданное лучше не редактировать без крайней необходимости!</div><br>

<table>
  <thead>
    <tr>
      <th>Сумма</th>
      <th>user.id</th>
      <th>user.email</th>
      <th>Операция</th>      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cash_operations.each do |cash_operation| %>
      <tr>
        <td><%= cash_operation.sum %></td>
        <td><%= cash_operation.try(:user).try(:id) %></td>
        <td><%= cash_operation.try(:user).try(:email) %></td>
        <td><%= cash_operation.operation_name %></td>        
        <td><%= link_to 'Показать', [:admin, cash_operation] %></td>
        <td><%= link_to 'Редактировать', edit_admin_cash_operation_path(cash_operation) %></td>
        <td><%= link_to 'Удалить', [:admin, cash_operation], method: :delete, data: { confirm: 'Вы уверены?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Новая операция с наличностью', new_admin_cash_operation_path %>
