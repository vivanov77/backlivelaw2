<h1>Платежи</h1>

<div style="color:red">ПРИМЕЧАНИЕ: Уже созданное лучше не редактировать без крайней необходимости!</div><br>

<table>
  <thead>
    <tr>
      <th>Сумма</th>
      <th>sender_id</th>
      <th>sender_email</th>
      <th>recipient_id</th>
      <th>recipient_email</th>
      <th>Заморожено</th>      
      <th>Назначение</th>
      <th>Создан</th>      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @payments.each do |payment| %>
      <tr>
        <td><%= payment.sum %></td>
        <td><%= payment.try(:sender).try(:id) %></td>
        <td><%= payment.try(:sender).try(:email) %></td>        
        <td><%= payment.try(:recipient).try(:id) %></td>
        <td><%= payment.try(:recipient).try(:email) %></td>
        <td><%= payment.cfrozen %></td>        
        <td><%= payment.typed_purpose %></td>
        <td><%= payment.created_at %></td>        
        <td><%= link_to 'Показать', [:admin, payment] %></td>
        <td><%= link_to 'Редактировать', edit_admin_payment_path(payment) %></td>
        <td><%= link_to 'Удалить', [:admin, payment], method: :delete, data: { confirm: 'Вы уверены?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Новый платёж', new_admin_payment_path %>
